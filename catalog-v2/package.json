/**
 * Package.json for Catalog v2
 * Independent module with its own dependencies
 */

{
  "name": "@nood/catalog-v2",
  "version": "2.0.0",
  "description": "NOOD Catalog v2 - Clean architecture catalog module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --test tests/*.test.ts",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ioredis": "^5.3.2",
    "axios": "^1.6.7",
    "zod": "^3.22.4",
    "pino": "^8.17.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.6",
    "@types/axios": "^0.14.0",
    "eslint": "^8.56.0",
    "@typescript-eslint/eslint-plugin": "^6.17.0",
    "@typescript-eslint/parser": "^6.17.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}